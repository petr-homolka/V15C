{
  "name": "@v15c/schema",
  "version": "0.1.0",
  "private": true,
  "description": "Datový model systému doprovázení — jen typy a cesty, bez závislostí",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json"
  }
}
