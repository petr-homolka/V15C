import * as React from 'react';
export interface TasksList02MobileProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const TasksList02Mobile: React.FC<TasksList02MobileProps>;
export default TasksList02Mobile;
