import * as React from 'react';
export interface FileIconsFolderProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const FileIconsFolder: React.FC<FileIconsFolderProps>;
export default FileIconsFolder;
